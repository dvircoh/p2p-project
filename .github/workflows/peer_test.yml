name: push test
on: [push]  # run on push
jobs:
  run-peer-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - run: python peet_test.py